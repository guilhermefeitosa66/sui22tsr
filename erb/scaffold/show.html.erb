<p></p>

<div class="ui container">
  <div class="ui menu"">
    <a href="#" class="item active"><strong><%= singular_table_name.titleize %></strong></a>
    <%%= link_to 'Novo', new_<%= singular_table_name %>_path, class: 'item' %>
    <%%= link_to 'Editar', edit_<%= singular_table_name %>_path(@<%= singular_table_name %>), class: 'item' %>
    <%%= link_to 'Voltar', <%= index_helper %>_path, class: 'item' %>
  </div>

  <% attributes.reject(&:password_digest?).each do |attribute| -%>
  <p>
    <strong><%= attribute.human_name %>:</strong>
    <%%= @<%= singular_table_name %>.<%= attribute.name %> %>
  </p>
  <% end -%>

</div>
